export enum ErrorActions {
    SET_ERROR = 'ERROR_ACTIONS/SET_ERROR',
}

export enum FireAuthErrorTypes {
    INTERNAL_ERROR = 'auth/internal-error',
    POPUP_CLODES = 'auth/popup-closed-by-user',
    USER_DISABLED = 'auth/user-disabled',
}
